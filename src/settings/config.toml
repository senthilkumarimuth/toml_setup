
[project]
name = "Toml Setup"
description = "Helps to config toml file properly"
author = "Senthilkumar"

[database]

# Nested `tables`
[environments]
  [environments.dev]
    ip = "0.0.0.0"
    port = 5000
    loglevel = 'DEBUG'

  [environments.production]
    ip = "10.0.0.3"
    loglevel = 'DEBUG'

